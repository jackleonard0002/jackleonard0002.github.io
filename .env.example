API_PORT=8787
FRONTEND_ORIGIN=*
FIREBASE_PROJECT_ID=whimsical-wands

# Comma-separated admin emails for backend role checks if custom admin claims are not set.
ADMIN_EMAILS=maxme009@gmail.com

# Option A (recommended local dev): set GOOGLE_APPLICATION_CREDENTIALS env var to service-account JSON path.
# Option B: set this JSON string directly.
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
